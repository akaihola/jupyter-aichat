The [keyring](https://pypi.org/project/keyring/) package is required
for saving the API key into the keyring of your system.
To install it, run the following command:

```bash
!pip install keyring
```
